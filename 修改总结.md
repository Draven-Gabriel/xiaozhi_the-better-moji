# 修改总结（按方面）

> 项目：Moji 桌面对话机器人 + xiaozhi-esp32-server 自建服务器


---

## 1. 语音对话体验（4 个方面）

| 方面 | 改前 | 改后 |
|---|---|---|
| **没声音** | 服务器用了没配置的讯飞语音合成，全部失败 | 换成免费 Edge 语音（晓晓女声），说话正常出声 |
| **音色突变** | 唤醒后的欢迎语是"缓存音频"，和正常对话声音不一样 | 欢迎语也走语音合成，全程一个声音 |
| **说话被打断** | 停顿超过 0.7 秒就被判定"说完了"，话没说完就被抢 | 放宽到 1.1 秒，正常停顿不再打断 |
| **抢话** | 环境噪音/自己播放的声音被识别成"嗯""啊"，机器人自问自答 | 过滤无意义短句（单字语气词直接丢弃），不抢话了 |

## 2. 工具调用（3 个方面）

| 方面 | 改前 | 改后 |
|---|---|---|
| **问天气** | 一定先反问"你在哪个城市" | 不问，直接按 IP 自动定位（失败用默认城市广州）播报 |
| **放音乐** | "放首歌"会反问"想听什么歌" | 直接随机播放；点歌名直接播 |
| **找歌** | 歌在子文件夹里就找不到；"放七里"匹配不到"七里香" | 只按文件名匹配 + 支持部分匹配 |


## 4. 记忆（2 个方面）

| 方面 | 改前 | 改后 |
|---|---|---|
| **记住事情** | 总结用的模型没配 key，记忆生成必然失败（只口头说"记住了"） | 改用 DeepSeek 总结，**真正写入数据库**，下次唤醒还记得 |
| **对话历史** | 不上报 | 每轮对话实时存入数据库（90+ 条） |

## 5. 系统配置（让一切跑起来的底座）

| 方面 | 说明 |
|---|---|
| **服务器↔智控台打通** | 密钥闭环、地址配置（WebSocket/OTA）、免登录认证 |
| **模型选型** | 默认模型设为 DeepSeek（有 key）；语音识别用本地免费模型；意图识别换 function_call |
| **本地开发环境** | 搭好 Python 3.11 环境 + 语音库 + 一键启动脚本，改代码即改即生效 |



---

## 一句话总结

让机器人**有声音、不抢话、不打断、记得住、放歌靠谱**，外加一套可以随便改代码的开发环境。



固件修改
1、boot键拓展，长按重置
2、自适应时间壁纸
3、优化字体显示
4、优化打断逻辑
5、核心打断（Barge-in）+ 软件回声消除
6、优化自动停止

