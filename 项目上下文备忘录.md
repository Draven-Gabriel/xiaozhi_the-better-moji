# 项目上下文备忘录（进度存档）

> 用途：压缩对话上下文，后续任何会话凭此文件即可恢复全部项目状态
> 最后更新：2026-08-25（文档重建版，原版因环境异常丢失，内容从对话记录恢复）

---

## 一、项目概况

- **产品**：Moji 桌面对话机器人（movecall moji-esp32s3，ESP32-S3，240×240 圆屏）
- **固件**：xiaozhi-esp32 v2.4.1 定制（PROJECT_VER=99.0.0，barge-in + 像素表情 UI）
- **服务器**：xiaozhi-esp32-server v0.9.6 全模块（Docker: web智控台/MySQL/Redis + 本地 Python server）
- **LLM**：DeepSeek deepseek-v4-flash（key 存智控台数据库）
- **目标**：差异化 = 记忆分层 + 对话工作流 + Tools/Skills 体系

## 二、环境与位置

| 项 | 位置 |
|---|---|
| 服务器代码 | `D:\vibe coding project\...\xiaozhi-esp32-server-main\main\xiaozhi-server\` |
| 部署目录（配置/venv/模型） | `D:\xiaozhi-esp32-server\` |
| 本地后端启动 | `D:\xiaozhi-esp32-server\start-local.ps1`（需 PATH: mamba\envs\xiaozhi\Library\bin） |
| 智控台 | `http://192.168.5.94:8002`（账号 ye / 密码 @YZYyzy050604） |
| WebSocket | `ws://192.168.5.94:8000/xiaozhi/v1/`（本地 server） |
| OTA（全模块） | `http://192.168.5.94:8002/xiaozhi/ota/` |
| 设备 | MAC 8c:bf:ea:1a:46:c4，绑定智能体"湾湾小何"（id 3b5f0a34b8f84ede8c6f9008f6c413ca） |
| MySQL | docker exec xiaozhi-esp32-server-db mysql -uroot -p123456 xiaozhi_esp32_server |
| Redis | docker exec xiaozhi-esp32-server-redis redis-cli（配置缓存键：model:data:*、server:config） |
| API token | 存于 %TEMP%\xz-token.txt；刷新=UPDATE sys_user_token |

## 三、已完成（2026-08-16 ~ 08-25）

- [x] 全模块部署 + 智控台注册 + server.secret 闭环（5194d657-cc44-448b-86eb-7590bb29bcca）
- [x] 双路验收（Ralph + 子代理，部署 9/9、Phase1 全过、天气改造全过）
- [x] 本地调试环境（venv 3.11 + ffmpeg + micromamba opus.dll + 模型 junction）
- [x] 设备连上、对话/音乐/记忆全链路通
- [x] Phase 0/1 完成（体验修复 + 天气免费化）
- [x] 前端修改版已部署（xiaozhi-web-local 镜像，20分钟前构建）
- [x] 仓库重组 + 备份（见第九节）

### 排障修复记录（已生效）

| 问题 | 根因 | 修复 |
|---|---|---|
| 没声音 | 智能体 tts_voice_id 残留讯飞音色；TTS 配置带 private_voice | 音色改 TTS_EdgeTTS0001；清 private_voice |
| 抢话 | 噪音/语气词被 ASR 识别成对话 | asr/base.py 加 is_meaningless_voice_text 过滤（源码改动 #1） |
| 说话被打断 | VAD min_silence 700ms 太短 | 1100ms |
| 记忆不持久 | 总结 LLM 用无 key 的 ChatGLM | Memory_mem_local_short → LLM_DeepSeekLLM（+清 Redis 模型缓存） |
| 音色变化 | 欢迎语走缓存音频 | enable_wakeup_words_response_cache=false |
| ASR 误判日文 | language=auto | ASR_FunASR language=zh |
| 天气反问城市 | desc 误导 LLM | get_weather.py desc 重写 |
| 放歌啰嗦/找不到歌 | desc 不明确 + 子目录匹配 bug | play_music.py desc 重写 + basename/包含匹配 |
| 天气付费 | 和风天气取消免费版 | **Open-Meteo 免费无 key 改造**（geocoding + forecast API + WMO 映射） |

## 四、Phase 1 状态（已完成 ✅）

- [x] 1.1 天气 desc 修复
- [x] 1.2 放歌 desc 修复
- [x] 1.3 歌名匹配 bug（basename+包含匹配）
- [x] 1.4 天气数据源 Open-Meteo 免费化
- [x] 1.5 记忆开启（mem_local_short + DeepSeek 总结）
- [x] 1.6 历史上报（chat_history_conf=2）
- [x] 双路核验通过（Ralph 6/6、子代理 7/7）

## 五、后续计划（详见《对话系统差异化优化计划.md》）

- **Phase 2 记忆分层**：摘要滚动 / L2 规范化 / L3 用户画像 / 注入装配 / 显式记忆工具
- **Phase 3 工作流**：上下文装配器 / 人设 prompt / TTS 后处理 / 防重复 / 观测
- **Phase 4 Tools+Skills**：moji 设备工具 / 提醒待办 / Skills 注册表 / 晨间问候等
- **Phase 5 上线**：删登录 / 构建 web 镜像 / 代码上 Docker / 回归备份
- **固件配套**：Moji MCP 工具（set_emotion/show_text/set_led）、表情动画、按键增强、8080 控制服务移植

## 六、关键经验（防坑）

1. 全模块配置以智控台数据库为准，改配置后**必须清 Redis 缓存**（model:data:*、server:config）
2. 智能体音色绑定在 ai_agent.tts_voice_id（不是模型表）
3. PowerShell 传 JSON 用文件方式（--data-binary @file）防转义
4. SQL 批量改库用 docker cp + sh -c "mysql < file"
5. 本地跑 server 需 PYTHONIOENCODING=utf-8（日志 GBK 崩溃）
6. 切回 Docker server 时 .config.yaml 的 url 要改回 xiaozhi-esp32-server-web:8002
7. 设备每次连接拉配置，改完配置重启设备即可生效
8. 日志中文乱码是 PowerShell 显示问题，实际 UTF-8 正常
9. venv 的 python.exe 是 launcher，实际解释器是 uv cpython（父子进程正常现象）
10. Docker Hub 本机不可达：web/server 镜像构建改用南大源 + 预构建 dist

## 七、验收记录（重建汇总）

- 部署总验收：**9/9 通过**（容器/配置/登录/参数/OTA/激活/LLM key）
- Phase 1 修改：Ralph 6/6 + 子代理 7/7（含功能测试：子目录匹配/包含匹配/不存在返回 None）
- 天气改造：双路全过（北京实测 29.7°C 毛毛雨/7天预报；不存在城市→None）
- 子代理发现并修复：DeepSeek key 占位符、ChatGLM 默认模型无 key、TTS 音色残留、记忆 LLM 配置

## 八、前端修改版（新版前端）

- 源码已归位 `xiaozhi-esp32-server-main/main/manager-web/`
- 特点：极光背景、渐变/粒子/铁磁流体组件、自定义 UI 组件库（UiButton/UiCard/UiInput）、Tailwind 设计系统
- 已部署：`xiaozhi-web-local:latest` 镜像（web 容器当前运行）
- 构建方式：Dockerfile-web-local（web_latest 南大源 + COPY dist）
- .npmrc 用 npmmirror 源；.env.production API=/xiaozhi

## 九、仓库发布状态（2026-08-25）

- **独立 git 仓库**：工作区 `new version 1.1`（提交：a87fbeb 全量、792f797 重组）
- **结构**：`docker-compose.yml`（部署入口）+ `README.md` + `xiaozhi-esp32/`（仅 moji 板型）+ `xiaozhi-esp32-server-main/` + `platform/`
- **别人部署**：mkdir server-data → 下载 model.pt → cp 配置模板 → docker compose up -d --build → 注册 → 配 secret → 重启
- **备份**：`D:\xiaozhi-backup-20260825\`（源码 zip 211MB、服务端 zip 14MB、数据库 SQL、配置；restore/backup 含 8 个 moji 固件 bin + manager-web-2026-08-25 官方前端备份）
- **固件 bin**：restore/backup/merged-binary-moji-*.bin（8 个版本，用户编译产物，可发布）
- **待办**：npm build dist 提交仓库 → compose 改用本地 dist 方案 → 验证构建 → 清理 cache

## 十、关键文件索引

| 文件 | 说明 |
|---|---|
| `README.md`（仓库根） | 别人部署文档 |
| `docker-compose.yml`（仓库根） | Docker 部署入口（构建版） |
| `修改总结.md` | 面向人话的修改汇总 |
| `start-local.ps1` | 本地后端启动脚本 |
| `D:\xiaozhi-backup-20260825\` | 全量备份 |
